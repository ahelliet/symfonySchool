{% extends 'base.html.twig' %}

{% block body %}
	<ol class="breadcrumb">
		<li class="breadcrumb-item">
			<a href="{{ path('accueil') }}">Accueil</a>
		</li>
		<li class="breadcrumb-item">
			<a href="{{ path('classe') }}">Toutes les classes</a>
		</li>

		{% if classe.id %}
			<li class="breadcrumb-item active">Modifier le nom de la classe</li>
		{% else %}
			<li class="breadcrumb-item active">Créer une nouvelle classe</li>
		{% endif %}
	</li>
</ol>

{{ form_start(form)}}
{{ form_row(form.nom)}}
<div class="form-group">
	<button style="margin-top: 15px" class="btn btn-primary form-control" type="submit">Ajouter une classe</button>

</div>
{{ form_end(form)}}{% endblock %}
